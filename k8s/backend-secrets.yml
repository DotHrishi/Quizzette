# Apply this BEFORE other manifests.
# Replace the placeholder values with real base64-encoded secrets:
#   echo -n "your-value" | base64
#
# DO NOT commit real secret values — this file is a template.
apiVersion: v1
kind: Secret
metadata:
  name: quizzete-backend-secrets
type: Opaque
data:
  DATABASE_URL: REPLACE_WITH_BASE64
  GROQ_API_KEY: REPLACE_WITH_BASE64
  JWT_SECRET: REPLACE_WITH_BASE64
