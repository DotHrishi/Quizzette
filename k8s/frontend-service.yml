apiVersion: v1
kind: Service
metadata:
  name: quizzete-frontend
  labels:
    app: quizzete-frontend
spec:
  selector:
    app: quizzete-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
